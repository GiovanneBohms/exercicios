#include <stdio.h>

main(){
    int x,y,multiplicacao;

    printf("digite dois numeros \n");
    scanf("%d %d",&x, &y);
    multiplicacao = x*y;
    printf("a multiplicacao deu %d", multiplicacao);
}